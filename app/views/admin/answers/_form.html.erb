<%= form_with(model: [:admin, @question, @answer], local: true, class: "ui form") do |form| %>
<%= render 'shared/messages'%>

  <div class="field">
    <%= form.label :body, cols: 7, rows: 3 %>
    <%= form.text_area :body %>
  </div>

  <div class="field">
    <%= form.label :correct %>
    <%= form.check_box :correct %>
  </div>

  <div class="actions">
    <%= form.submit class: "ui primary red button" %>
  </div>
<% end %>
