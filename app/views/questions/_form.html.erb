<% if action_name == 'new'
     resource = [@question.test, @question]
   else
     resource =  @question
     end
%>

  <%= form_for resource, html: { class: "ui form" } do |f| %>
    <div class="field">
    <%= f.text_field :body %>
  </div>
  <%= f.submit class: "ui button" %>
<% end %>